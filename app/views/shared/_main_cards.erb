<div class="card-test" style="background-image: linear-gradient(rgba(0,0,0,0), rgba(0,0,0,0.5)), url('<%= image_path test.img_name, height: 500, width:500, crop: "fit" %>')">

    <p class="category-label">
      <%= @tests.where(category: test.category).count %> tests disponnibles
    </p>

    <div class="card-test-content">
      <h3 class="title-inter"> <%= test.category.html_safe %> </h3>

      <%# <p>= description</p> %>
      <%= link_to "Voir tous les tests", tests_path(test_id: test.id), class: "btn-principal-white", data: { turbo_frame: :tests_by_category, controller: "scroll", action: "click->scroll#scroll", target: "target-section" } %>

    </div>
</div>
